# Hosts to deploy onto. Copy, edit, and pass with -i.
#
#   ansible-playbook -i inventory.ini playbook.yml

[scanners]
# scanner-1.example.com
# 192.168.1.50 ansible_user=ubuntu

[scanners:vars]
# amazingscanner_port=8080
# amazingscanner_threads=8
# container_packages=['docker.io']
